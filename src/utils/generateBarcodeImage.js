// const { createCanvas } = require("canvas");
// const JsBarcode = require("jsbarcode");

// Canvas module removed - this function is not currently used
module.exports = function generateBarcode(orderId) {
  // Placeholder - canvas was removed to avoid build issues
  // If you need barcode generation, consider using a lighter library
  return "data:image/png;base64,placeholder";
};
